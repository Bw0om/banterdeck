---
import Category from '../../../components/pages/Category.astro';
import { catSections } from '../../../lib/content';
export function getStaticPaths() {
  return catSections.map((s) => ({ params: { id: s.id }, props: { s } }));
}
const { s } = Astro.props;
---
<Category s={s} lang="no" />
