---
import Redirect from '../../components/Redirect.astro';
import { games } from '../../lib/content';
export function getStaticPaths() {
  return games.map((g) => ({ params: { slug: g.slug } }));
}
const { slug } = Astro.params;
---
<Redirect to={`/no/drinking-games/${slug}`} />
